# Packaging Directory

Version metadata and packaging configuration files.

## Contents

| Item | Description |
|------|-------------|
| `packaging/version.json` | Application version and release metadata |

This directory is **not bundled** into the PyInstaller executable.
It holds build-time configuration for versioning and release
management.
