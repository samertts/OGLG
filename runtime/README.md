# Runtime Directory

Runtime configuration and state files for runtime operation.

## Contents

| Item | Description |
|------|-------------|
| `runtime/` | Runtime config files and state persistence |

This directory is **not bundled** into the PyInstaller executable.
It exists at the project root for runtime state persistence
during development.
