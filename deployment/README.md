# Deployment Directory

Environment-specific deployment configurations.

## Contents

| Item | Description |
|------|-------------|
| `deployment/` | Environment configs, deployment manifests |

This directory is **not bundled** into the PyInstaller executable.
It holds deployment environment configuration files, manifests,
and environment-specific overrides.
